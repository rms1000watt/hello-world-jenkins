pipeline {
    agent {
        docker {
            image 'docker:17.12.0-ce-git'
            args '-v /var/run/docker.sock:/var/run/docker.sock'
        }
    }
    stages {
        stage('Build NodeJS') { 
            steps {
                sh("pwd && \
                    rm -rf dummy-nodejs-project && \
                    git clone https://github.com/rms1000watt/dummy-nodejs-project.git && \
                    cd dummy-nodejs-project && \
                    ./build.sh")
            }
        }
        stage('Build Golang') { 
            steps {
                sh("pwd")
            }
        }
        stage('Test') { 
            steps {
                echo 'Testing Done'
            }
        }
    }
}
